/* ==========================================================================
   dashboard.js — Dashboard: opslaan in en laden vanuit localStorage
   Stap 5 vult deze functies volledig in; stap 3 gebruikt al de signaturen.
   ========================================================================== */

/* exported saveToDashboard, loadDashboard */

// Slaat een geluid op in het dashboard (tijdelijk: stap 5 vervangt dit).
const saveToDashboard = (sound) => {
   console.log('Toevoegen aan dashboard:', sound.name);
};

// Laadt en toont de opgeslagen dashboard-knoppen uit localStorage (stap 5).
const loadDashboard = () => {
   // Wordt ingevuld in stap 5
};
